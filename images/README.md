Put your project images here (screenshots, exported maps, photos).

Recommended: JPG for photos/screenshots, PNG for anything with sharp lines
(diagrams, tables). Keep images under ~500KB each so the site loads fast —
export maps at 1500px wide max, or compress with squoosh.app.
